# Go to home folder (optional)
cd ~

# Clone the repo (skip if already cloned)
git clone https://github.com/nikologabro-lang/Niko-s-MT.git

# Go into the repo
cd Niko-s-MT

# Make the script executable
chmod +x NMT

# Run the script
./NMT
